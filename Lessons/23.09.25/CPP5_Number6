#include <iostream>
#include <cstdlib>

int main() {
    int n = 7;
    int m = 7;
    int mas[n][m];

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            mas[i][j] = std::rand()%10; 
            std::cout << mas[i][j] << " ";
        }
        std::cout << std::endl;
    }

    int sum = 0;
    int diag = (n < m) ? n : m; 
    for (int i = 0; i < diag; i++) {
        sum += mas[i][i];
    }

    std::cout << "Сумма элементов главной диагонали: " << sum << std::endl;
}